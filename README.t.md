# README TEMPORAL

1. Ver contenido SIN descomprimir (rápido)

tar -tf archivo.tar.gz

2. Descomprimir

tar -xzvf archivo.tar.gz

Descomprime a una carpeta

tar -xzvf archivo.tar.gz -c

3. Si usaste .tar.xz

tar -xJvf archivo.tar.xz

Regla rápida
archivo	comando
.tar.gz	tar -xzvf
.tar.xz	tar -xJvf
